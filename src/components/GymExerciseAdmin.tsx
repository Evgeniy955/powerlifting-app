'use client'
import { useState } from 'react'
import { Button, Card, Input } from '@/components/ui'
export function GymExerciseAdmin({initial}:{initial:{id:string;name:string;category:string|null;_count:{exercises:number;maxes:number}}[]}){const [items,setItems]=useState(initial);const [name,setName]=useState('');const [category,setCategory]=useState('');async function add(){if(!name.trim())return;const r=await fetch('/api/admin/gym-exercises',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,category})});if(r.ok){setItems([...items,await r.json()]);setName('');setCategory('')}}return <Card className="space-y-4"><div className="grid gap-2 sm:grid-cols-[1fr_1fr_auto]"><Input placeholder="Название упражнения" value={name} onChange={e=>setName(e.target.value)}/><Input placeholder="Категория" value={category} onChange={e=>setCategory(e.target.value)}/><Button onClick={add}>Добавить</Button></div><div className="divide-y divide-border">{items.map(x=><div key={x.id} className="flex justify-between py-2 text-sm"><span>{x.name}<span className="ml-2 text-text-secondary">{x.category??''}</span></span><span className="text-xs text-text-secondary">{x._count.exercises} в планах</span></div>)}</div></Card>}

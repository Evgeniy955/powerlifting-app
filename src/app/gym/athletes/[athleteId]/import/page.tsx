'use client'
import { use } from 'react'
import { useState } from 'react'
import Link from 'next/link'
import { Card, Input } from '@/components/ui'
import { createClient } from '@/lib/supabase/client'
export default function GymImportPage({params}:{params:Promise<{athleteId:string}>}){const {athleteId}=use(params);const [status,setStatus]=useState('');async function upload(file:File){if(file.size>10*1024*1024){setStatus('Файл больше 10 МБ');return}const allowed=['application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document'];if(!allowed.includes(file.type)){setStatus('Поддерживаются только PDF и DOCX');return}const path=`${athleteId}/imports/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const {error}=await createClient().storage.from('assessments').upload(path,file);if(error){setStatus('Не удалось загрузить файл');return}const r=await fetch(`/api/gym/athletes/${athleteId}/assessments`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fileName:file.name,mimeType:file.type,storagePath:path})});setStatus(r.ok?'Документ импортирован и доступен в профиле клиента':'Не удалось сохранить импорт')}return <main className="mx-auto min-h-[calc(100vh-3.5rem)] max-w-2xl space-y-5 bg-bg p-6 text-text-primary"><Link href={`/gym/athletes/${athleteId}/plans`} className="text-sm text-text-secondary">← Планы клиента</Link><h1 className="font-display text-xl uppercase">Импорт тренировки</h1><Card className="space-y-3"><p className="text-sm text-text-secondary">Загрузите план или анкету в формате DOCX или PDF. Документ сохранится в профиле клиента.</p><Input type="file" accept="application/pdf,.pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,.docx" onChange={e=>{const file=e.target.files?.[0];if(file)void upload(file)}}/>{status&&<p className="text-sm text-text-secondary">{status}</p>}</Card></main>}

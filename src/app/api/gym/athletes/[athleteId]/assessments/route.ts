import { NextResponse } from 'next/server'
import { prisma } from '@/lib/prisma'
import { requireUser } from '@/lib/session'
import { assertGymClientAccessible } from '@/lib/authorization'
export async function POST(req:Request,{params}:{params:Promise<{athleteId:string}>}){const user=await requireUser();const {athleteId:clientId}=await params;await assertGymClientAccessible(clientId,user);const b=await req.json() as {fileName?:string;mimeType?:string;storagePath?:string};if(!b.fileName||!b.storagePath)return NextResponse.json({error:'Не указан файл'},{status:400});if(!/^(image\/(png|jpeg|webp)|application\/pdf|application\/vnd\.openxmlformats-officedocument\.wordprocessingml\.document)$/.test(b.mimeType??''))return NextResponse.json({error:'Недопустимый тип файла'},{status:400});const record=await prisma.gymClientAssessment.create({data:{clientId,fileName:b.fileName.slice(0,255),mimeType:b.mimeType!,storagePath:b.storagePath.slice(0,500)}});return NextResponse.json(record,{status:201})}

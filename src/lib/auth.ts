// Deprecated: authentication now runs through Supabase Auth.
// See src/lib/supabase/{client,server}.ts, src/middleware.ts, and
// src/app/auth/callback/route.ts (first-login role/invite provisioning
// used to live here as a NextAuth `createUser` event).
export {}
